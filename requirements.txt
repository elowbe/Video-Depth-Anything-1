numpy==1.24.0
torch==2.1.1
torchvision==0.16.1
opencv-python
matplotlib
pillow
imageio==2.37.0
imageio-ffmpeg==0.4.7
decord
# PyTorch SDPA is used on both CUDA and MPS. xFormers remains an optional
# CUDA-only acceleration package and is intentionally not required on macOS.
einops==0.4.1
easydict
tqdm
OpenEXR==3.3.1
